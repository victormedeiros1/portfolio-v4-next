import { LoadingScreenStyles, Bar, Progress } from './styles';

const LoadingScreen = () => (
  <LoadingScreenStyles>
    <Bar>
      <Progress />
    </Bar>
  </LoadingScreenStyles>
);

export default LoadingScreen;
